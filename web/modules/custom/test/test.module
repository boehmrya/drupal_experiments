<?php

/**
 * Implements hook_theme().
 */
function test_theme($existing, $type, $theme, $path) {
  return [
    'my_template' => [
      'variables' => [
        'test_var' => [],
      ],
    ],
  ];
}


/**
 * Implements hook_preprocess_HOOK().
 */
function test_preprocess_block(&$variables) {
  $blocks = $variables['content'];
  kpr($blocks);
  /*
  foreach ($blocks as $block) {
    dpm($block);
  }
  */
}

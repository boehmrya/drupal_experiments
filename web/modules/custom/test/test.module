<?php

/**
 * Implements hook_theme().
 */
function test_theme($existing, $type, $theme, $path) {
  return [
    'my_template' => [
      'variables' => [
        'test_var' => [],
      ],
    ],
  ];
}


function test_block_view_block_content_alter(array &$build, \Drupal\Core\Block\BlockPluginInterface $block) {
  dpm($build['#block']->label());
  // Change the title of the specific block.
  //$build['#block']['#settings']['label'] = t('New title of the block');
}
